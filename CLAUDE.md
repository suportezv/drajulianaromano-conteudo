# Dra. Juliana Romano Conteúdo Studio (memória persistente do projeto)

Este repositório é o **Dra. Juliana Romano Conteúdo Studio**: edição e agendamento de conteúdo para as redes da **Dra. Juliana Romano**. Estúdio irmão do `ana-conteudo`, `eita-conteudo` e demais estúdios da agência; a infraestrutura é idêntica, o posicionamento é o desta marca.

> Bootstrap: este repo foi criado vazio e inicializado em 2026-08-18 a partir da infraestrutura compartilhada do `eita-conteudo` (que herda do `ana-conteudo`). Os campos marcados **PENDENTE** são específicos desta marca e ainda não foram fornecidos/validados — preencher e commitar assim que confirmados.

**Antes de editar qualquer vídeo ou escrever qualquer caption, leia `FRAMEWORK.md`** (posicionamento, regras inegociáveis, formatos, assinaturas de edição e todos os gotchas técnicos).

## Regras que valem em qualquer resposta pública

- Nunca usar travessão em texto público (caption, lettering, legenda): reescrever a frase.
- Quando citar a criadora: sempre **"Neurocientista criadora do método EITA"**.
- Palavrão em vídeo **se bipa, não se corta**.
- Loudness final: **-14 LUFS**.

## Working dirs

- Estúdio: este repo (symlink `~/drajulianaromano-conteudo` aponta para cá). Projetos em `projects/<nome>/`.
- Ferramentas: `video-use` e `hyperframes` clonados em `/workspace/browser-use/` e `/workspace/heygen-com/` (Linux/cloud) ou `~/video-editor/` (Mac). Skills registradas em `~/.claude/skills/`.
- Ambiente novo (container limpo): rode `bash scripts/setup.sh` e depois `bash scripts/validate.sh`.
- **Em sessão nova, conferir `ls /workspace` antes de contar com video-use ou hyperframes** e rodar o `setup.sh` à mão se estiver vazio (ver gotcha do campo de setup do environment).
- Cinto de ferramentas (portado do `profissioai-conteudo` em 2026-09-18, ver seção própria abaixo): `scripts/*.py` (decupagem por âncora, LUT S-Log2, ZIP remoto, imagem por IA, upload Drive), `remotion/` (cartões de título em React, paleta em `remotion/src/marca.ts`) e `assets/fonts/` (Playfair Display e Montserrat locais).

## IDs e contas

- Marca pública: **"Dra. Ju Romano | Bella & Magra"**, CRM-SP 138885, RQE 5246. Emoji assinatura nos títulos: 🌻. Público: mulheres 35+ (bio oficial do canal); assinatura pessoal "Mãe, Médica e CEO"; especialidade nutrologia e emagrecimento feminino; temas: emagrecimento, obesidade, canetas (Mounjaro/tirzepatida, citada como "Dona Tirz&patida" em Shorts), efeito sanfona, menopausa, lipedema, sarcopenia.
- Instagram da marca: **@drajulianaromano**; TikTok **@drajulianaromano**; YouTube canal **UCMDg2knRYXE0QwazYh5bcVA** (~2.960 inscritos em set/2026, mistura de Shorts diários e vídeos longos).
- Metricool: conta da agência **suporte@zavi.ag** (conector MCP autorizado e validado em 2026-08-18). Marca no painel: **"drajulianaromano", blog_id 6741531** (validado via getBrandSettings em 2026-09-01; Instagram, TikTok e YouTube conectados), timezone America/Sao_Paulo.
- **Melhores dias e horários, medidos em 2026-09-21** (`getBestTimeToPostByNetwork`, janela de uma semana; reconferir a cada 2 ou 3 meses). Os três canais concordam nos dias: **quarta, quinta e sexta** formam o miolo (índice 96 a 100), segunda e terça ficam ~20% abaixo, e **fim de semana cai quase pela metade** (sábado 48 a 58, domingo 43 a 55, o pior dia em todas as redes). Os horários divergem por rede: **Instagram pica às 10h todos os dias** (sexta 10h é o pico absoluto), com 11h e 12h logo atrás; **TikTok também pica às 10h** e tem um segundo pico forte às **18h** (quinta 10h é o topo); **YouTube pica às 16h todos os dias da semana** (quarta 16h e quinta 16h empatam no topo), com 15h e 10h em seguida. Ou seja: Short/Reel de manhã (10h), TikTok pode repetir às 18h, e vídeo longo às 16h de quarta ou quinta, que é a cadência já em uso. **Atenção**: em 2026-09-01 a marca "drjulianofratezi" (blog_id 6741542) estava com o Instagram da drajulianaromano conectado por engano; conferir no painel.
- **Regra de agendamento (todas as marcas da agência)**: sempre incluir TODOS os canais conectados da marca no post, exceto YouTube horizontal. YouTube entra como **Short** (`youtubeData: {type: "short", title, madeForKids: false}`); Instagram como REEL; Facebook como REEL; TikTok, LinkedIn e Pinterest com networkData padrão. Nunca publicar vídeo vertical como YouTube horizontal comum.
- Kairogen: conta suporte@zavi.ag. Em 2026-08-18 o plano estava **FREE com 0 créditos** (contratação de plano pago a caminho, confirmar antes de usar para b-roll).
- ElevenLabs: chave `sk_` (51 chars) na env var `ELEVENLABS_API_KEY` do environment; o setup grava em `.env` na raiz do video-use. Validada em 2026-08-18: TTS, STT Scribe (timestamps por palavra), sound-generation e voices_read todos OK. **Atenção: plano free (10.000 chars/mês, sem instant voice cloning)**; para produção contínua e voz clonada da marca, precisa de upgrade. Voz da marca para narração: **PENDENTE (voice_id, modelo e settings)**.
- Drive (brutos): vídeos longos na pasta pública `1fkXwabNwAX9jkoR-Po5tCSYdHq0kjgU4` (Drive da cliente, drajhromano@gmail.com; suporte@zavi.ag tem só LEITURA nas pastas da cliente, sem copiar/mover para lá). **Capas prontas: pasta oficial `1mR7g9h-SmryaQU5Zbpn8WfcpQ5UoQ54w`** ("qualquer pessoa com o link: leitor" desde 2026-09-04) — exportou do Design, arrasta para lá e o estúdio baixa sozinho.
- Fotos da cliente: pasta pública "FOTOS DRA JU" `1J0m_WyvZpqb3mt7sMmpFqvP2mbeQDOy7` com 15 retratos editoriais profissionais (blazer branco/preto/vinho; ids catalogados na conversa de 2026-09-04; a #18004CM59gqY415qC7Qm8vASzktHHuRgt, blazer branco mão no queixo, foi a da capa da menopausa). O conector não lista essa pasta (dono externo): raspar o HTML público da pasta com curl e regex `data-id=`. Pastas de ensaio PRIVADAS (só por conta, arquivos 10-24 MB, conector não transfere): "EnsaioCarlosDra Juliana Romano" (~100 fotos FCS, com as poses de choque das capas de referência), ENSAIO JU ROMANO, retratos profissionais — pedir "qualquer pessoa com o link: leitor" quando precisar delas.

## Gotchas essenciais (herdados do ana-conteudo/eita-conteudo, todos validados)

- Brutos de iPhone são HLG 10-bit: gerar proxy SDR uma vez antes de editar (filtro `colorspace=all=bt709:itrc=bt2020-10:iprimaries=bt2020:ispace=bt2020nc`).
- Legendas SEMPRE por último no filter chain; overlays via PIL em PNG sequence + qtrle (ou PNG estático com fade de alpha).
- Zoom animado com `zoompan`, não `crop` (crop não aceita `t` em w/h).
- **O patch `video-use-is-portrait-source` foi aposentado (18/set/2026).** O upstream reescreveu `is_portrait_source` para ler também o `rotation` do side data. O `validate.sh` testa **comportamento** (retrato, paisagem e paisagem com matriz de rotação 90) em vez de procurar o patch; o arquivo em `patches/` fica só como histórico.
- Metricool MCP: sem delete (cancelar = update draft:true; update devolve id novo); mídia por URL pública (o Metricool copia para o CDN dele na hora).
- Mac: usar ffmpeg-full keg-only com PATH explícito. Linux/cloud: o apt responde 403 com network Custom; o `setup.sh` instala o build estático do BtbN (gpl, com `subtitles`, `zscale`, `lut3d`, `zoompan`, `loudnorm`) em `/usr/local/bin`.
- Cloud, brutos do Drive: environment com network Custom e `drive.google.com` + `drive.usercontent.google.com` + `api.elevenlabs.io` liberados. **Cada estúdio tem environment próprio**; o desta marca é `drajulianaromano-conteudo` (`env_01Xq5rLD9d9CkcogJEj6kbMU`). Download direto de arquivo público, qualquer tamanho: `curl -L "https://drive.usercontent.google.com/download?id=<ID>&export=download&confirm=t"`. O conector MCP do Drive serve para busca e metadados; download por ele só até ~4 MB. Fallback para arquivo público pequeno: Kairogen `download_audio_from_url`.
- Cloud, mídia pública para o Metricool: commit temporário do render na branch (repo público, raw.githubusercontent.com passa no proxy), agendar e remover o arquivo em seguida. Exige `git add -f` (o .gitignore barra mídia) com autorização do usuário. **Por isso este repo deve ser público.**
- Cloud: a env var `ELEVENLABS_API_KEY` de environment antigo continha um key ID (64 hex); a chave real é `sk_...` de 51 caracteres.
- Ver imagem de URL bloqueada pelo environment (ex.: thumbnails i.ytimg.com): Kairogen `download_image_from_url` baixa server-side e retorna a imagem renderizada na conversa. Validado em 2026-09-01 com hqdefault.jpg (~480x360; maxresdefault pode estourar o limite de tokens).
- Agendamento YouTube validado em 2026-09-01 (post 369746340): vídeo direto pela URL pública do Drive (`drive.usercontent.google.com/download?id=<ID>&export=download&confirm=t`) e `videoThumbnailUrl` por raw.githubusercontent.com funcionam; o Metricool copia ambos para o CDN dele na hora (não precisa vincular o Drive no Metricool). raw.githubusercontent bloqueado localmente no environment não importa: quem busca é o servidor do Metricool. getBestTimeToPostByNetwork: dayOfWeek 1=segunda ... 7=domingo. Canal publica quase diariamente sem padrão fixo de dia/horário (levantado em 2026-09-02): usar sempre o melhor horário do Metricool. Agendados: Mounjaro qui 03/09 16h (post 369746340), Razões da Obesidade ter 08/09 16h (post 369940826; segunda 07/09 é feriado e terça pontua mais alto) Menopausa qui 10/09 16h (post 371279170, capa feita no Claude Design pela agência) e Menopausa 2 qui 17/09 16h (post 376552659; agendado errado em sexta 18/09 e corrigido em 2026-09-15 via update, que troca o id; capa do fluxo briefing+GPT, título "Come pouco e não emagrece?"). LIÇÃO: sempre validar o dia da semana com `date -d <data> +%A` antes de agendar. Cuidado Mounjaro qui 24/09 16h (post 376554817, título "Mounjaro sem resultado?", capa do fluxo briefing+GPT em layout editorial branco/azul). Segredo da Obesidade qui 01/10 16h (**post 378341455**, id novo depois da troca de capa; uuid 1932839351931913950; título "Seu corpo não quer emagrecer, ele quer te proteger"). Primeira capa gerada pelo próprio estúdio via API, em três rodadas: v1 com a foto do ensaio como base, sem expressão; v2 gerada com expressão de acolhimento; **v3, a escolhida pelo usuário, com expressão de revelação** (olhos arregalados, palma apresentando o cadeado). Cada troca de capa gera id novo: 378336192, depois 378341455, e **378344220 é o id atual**. Renders em `design-system/renders/capa-segredo-obesidade.jpg` (a agendada) e `capa-segredo-obesidade-alt-acolhimento.jpg` (a alternativa fiel). Melhor horário medido em 18/09: quarta 16h (12033) e quinta 16h (11979) empatam na prática, então a cadência semanal de quinta pesa mais que a diferença. GOTCHA (2026-09-15, e o inverso em 2026-09-18): o normalizador de mídia do Metricool recusa uma das duas formas de URL do Drive com "Failed to normalize media", e **qual das duas falha muda de dia**. Em 15/09 caiu a `drive.usercontent` e passou a `drive.google.com/file/d/<ID>/view`; em 18/09 foi o contrário, a `/view` falhou e a `drive.usercontent...&confirm=t` passou de primeira. Regra: se der "Failed to normalize media", tentar a outra forma antes de procurar problema no arquivo. Thumbnail: limite do YouTube é 2 MB — converter export do Design (2560x1440 PNG ~5 MB) para 1280x720 JPG q92 antes de agendar.
- **REGRA PERMANENTE de capa (decidida pelo usuário em 2026-09-18): nunca colar foto pronta, sempre GERAR a imagem.** As fotos do ensaio no Drive entram apenas como **referência de semelhança** (`--ref`), nunca como recorte colado. O prompt tem que pedir **expressão e gesto condizentes com a tese do vídeo**: pose neutra faz capa fraca. O rosto dela está **aprovado** desde 2026-09-18, então não é mais preciso validar caso a caso; o que continua obrigatório é o QC de fidelidade abaixo.
- **QC de capa gerada: comparar o rosto lado a lado com a foto real, sempre.** Montar uma tira com o recorte do rosto real e o de cada variante (exemplo em `design-system/renders/qc-rostos-segredo-obesidade.jpg`). Achado de 2026-09-18 que vale como regra: **quanto mais exagerada a expressão, maior a deriva de identidade**. Na mesma leva, com os mesmos dois `--ref`, a variante de mão no peito manteve o rosto dela (olhos, nariz, textura de pele e marcas de expressão reais), enquanto a de olhos arregalados e a de palma erguida engrossaram a sobrancelha, alisaram a pele e arredondaram o rosto, virando "uma mulher parecida". Conferir também mãos (cinco dedos, unhas e anel iguais aos da foto) e ortografia com acento.
- **Preferência do cliente quando fidelidade e força da expressão brigam: vence a expressão.** Em 2026-09-18 o estúdio recomendou a variante mais fiel (mão no peito) e o usuário escolheu a mais agressiva (olhos arregalados, palma apresentando o prop), ciente da deriva de rosto. Regra prática daqui em diante: gerar sempre a variante expressiva forte como candidata principal, apresentar a fiel como alternativa, e **não travar a entrega por causa da deriva**; só reportar quando ela for grande a ponto de não parecer mais a mesma pessoa.
- **Fluxo de capa por API (2026-09-18, capa Segredo da Obesidade): o melhor até agora, e o estúdio faz sozinho.** `scripts/gera_imagem.py` ganhou `--ref` (foto de referência): a OpenAI passa a usar `/v1/images/edits` e o Gemini recebe a foto como parte inline. Receita validada: escolher no Drive a foto do ensaio cuja **pose case com a tese do vídeo** (aqui, mãos na cintura, blazer preto, para o tema da gordura como armadura), reduzir para 1024 px no lado maior, e mandar um prompt único com cena inteira (pessoa recortada à direita, fundo escuro temático, luz de contorno, bloco de texto caps com contorno preto, palavra-chave em gradiente dourado, faixa vermelha, prop 3D e uma seta). Prompt em `design-system/CAPAS-prompt-api.txt`. Duas ou três fotos de ângulos diferentes como `--ref` sustentam a semelhança melhor que uma. Cada geração 1536x1024 leva ~1min25. Comparação real: **`gpt-image-2` ganhou do `gemini-3-pro-image`** em tipografia (maior e mais pesada), no prop (cadeado com segredo visível) e na luz; o Gemini entrega 16:9 nativo (1376x768) e o OpenAI só 3:2 (1536x1024), que cortado para 1536x864 mantém a composição porque o prop foi pedido vazando pelo rodapé. Custo: ~1min20 no OpenAI, ~19s no Gemini. QC obrigatório de rosto e mãos ampliados: nas duas o rosto saiu fiel e as mãos com cinco dedos e a aliança da foto original, mas **isso precisa ser conferido toda vez**.
- **Fluxo de capa vencedor anterior (2026-09-14, capa Menopausa 2), quando não houver API de imagem**: o estúdio escreve o BRIEFING CRIATIVO (conceito com paradoxo, cena, foto do ensaio anexa por URL pública, tipografia, selo, prop, uma única seta com semântica, paleta fechada, regras) e a agência executa em modelo de imagem externo (GPT); a cena inteira gerada de uma vez integra luz/foto/prop melhor que qualquer colagem por camadas. Estúdio recebe o PNG pela pasta oficial de capas, faz QC (ortografia, sem travessão, rosto aprovado pela Dra., 1280x720 JPG <=2MB) e agenda. ATENÇÃO no QC: a IA re-renderiza o rosto e inventa detalhes (unhas, joias); validar com a cliente. Template do briefing: mensagem de 2026-09-14 na sessão; composição procedural local (foto real + placa PIL + tipografia HTML/playwright, scripts no scratchpad) é FALLBACK quando não houver modelo de imagem disponível.
- Fluxo de capa anterior (validado na capa da Menopausa, 2026-09-04): assets via Higgsfield (fundo nano_banana_pro + remove_background da foto e do prop) -> composição no sandbox_exec com `design-system/scripts/capa_menopausa.py` (supersampling 2x) OU execução no Claude Design pela agência com o prompt `design-system/CAPAS-prompt-design.md` + os 3 assets anexados (a arte aprovada da Menopausa saiu do Design). Sandbox: stdout trunca ~20K chars (elide o meio acima disso); arquivos somem ~10s após cada call (background:true dá lease de 15 min); trazer imagem do sandbox para o environment via base64 em chunks funciona mas é frágil, preferir CDN + browser do usuário para aprovação. Kairogen download_image_from_url NÃO aceita os CDNs do Higgsfield (host not allowed). Imagem colada no chat chega só renderizada (sem arquivo em disco): arte final produzida fora do estúdio precisa chegar por URL pública (pasta oficial de capas).
- Render de capa local: fontes do Google Fonts bloqueadas no environment; instalar TTFs do repo público google/fonts (sparse clone) em ~/.fonts + fc-cache, e renderizar o artboard com playwright-core + Chromium de /opt/pw-browsers/chromium (screenshot 1280x720). Frames de vídeo verticalmente: cortar acima da faixa de legenda queimada (~y 770 em 1080p) ou achar take com a mão erguida.
- Trilhas/SFX: ElevenLabs sound-generation (`/v1/sound-generation`, máx ~22s) gera beds e SFX ótimos; para trilha maior, gerar build+drop e costurar com acrossfade. Detecção de BPM/batidas: script próprio com numpy (fluxo de energia + autocorrelação), ver `ana-conteudo/projects/teste-02-interlagos/edit/beats.py`.
- **Brutos de Sony em S-Log2 (A7 III): converter, não "filtrar".** O XML lateral de cada clipe (`C00xxM01.XML`) declara `CaptureGammaEquation` e `CaptureColorPrimaries`; quando diz `s-log2`/`s-gamut`, a imagem chega chapada e precisa de conversão para Rec.709. `scripts/gera_lut_slog2.py` gera a LUT com a `colour-science` (log, linear, primárias, ombro, gama). Dois cuidados: **exposição -0,5 stop e joelho em 0,65**, senão o branco estoura; e conferir que o ffmpeg aplica a `lut3d` **em RGB, não em YUV** (ele auto-insere `yuvj420p -> rgb24`; verificar com `-v verbose`). Saída sempre com `out_range=tv` e `-color_range tv`.
- **Câmera pode gravar na vertical sem gravar a flag de rotação.** O arquivo vem 3840x2160 deitado e o ffprobe não mostra rotação; só olhando um frame se descobre. Corrigir com `transpose=1` antes de escalar. Checar um frame de qualquer lote novo antes de planejar o corte.
- **Decupagem por âncora de texto, não por timecode.** `scripts/decupar.py` recebe um `edl.json` onde cada trecho é "de tal frase até tal frase"; ele casa as âncoras contra a transcrição com timestamp por palavra do Scribe e resolve os tempos. Revisar um corte vira editar uma frase. O campo `apos` empurra o cursor quando a mesma frase aparece antes. `scripts/relatorio_decupagem.py` retranscreve as peças finais e prova que a fala aproveitada sobreviveu inteira.
- **Ler o índice de um ZIP gigante no Drive sem baixar o arquivo.** `drive.usercontent.google.com` aceita `Range`: pegar os últimos ~64 KB, achar o EOCD (e o ZIP64 em arquivo >4 GB), ler o central directory e listar tudo; entradas `stored` (exports do OneDrive com MP4) saem uma a uma por outro `Range`. `scripts/zip_index_remoto.py list|get <file_id>`.
- **Upload de vídeo para o Drive só com token pronto.** O conector MCP do Drive só aceita `base64Content` na própria chamada (inviável para vídeo). `www.googleapis.com` foi liberado na allowlist em 2026-09-18; `scripts/sobe_para_drive.py` precisa ainda de `GOOGLE_OAUTH_TOKEN` gerado fora (OAuth Playground, escopo `drive.file`, vale 1 h). Upload real ainda **não testado** nesta conta. Sem token, entrega sai por commit temporário na branch ou pelo envio direto na conversa.
- **Processo em background com `nohup`/`setsid` é recolhido quando a tool call retorna.** Usar `run_in_background: true` da própria ferramenta Bash (o harness rastreia) ou deixar estourar o timeout do primeiro plano. Em lote longo, `flock` num arquivo de lock evita corrida.
- **Remotion renderiza com o `headless_shell`, não com o Chromium do Playwright.** O `chromium-1194` removeu o headless antigo que o Remotion pede ("Old Headless mode has been removed"). O binário certo é `/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell`, fixado em `remotion/remotion.config.ts`. Baixar o browser próprio do Remotion está fora da allowlist. Validado aqui em 2026-09-18: `npx remotion still src/index.ts CartaoTituloVertical f.png --frame=110` em ~3 s a quente.
- **Remotion e HyperFrames resolvem o mesmo problema.** A ponte é de mão única (`remotion-to-hyperframes`). Ver "Escolha do framework de motion" no `FRAMEWORK.md`; cada peça declara o seu na primeira linha do briefing.
- **Licença do Remotion não é MIT.** Texto em `remotion/node_modules/remotion/LICENSE.md`: Free License para indivíduo, empresa com fins lucrativos de **até 3 funcionários**, organização sem fins lucrativos, ou quem está **avaliando e ainda não usa de forma comercial**; fora disso, Company License (remotion.pro). Quem se enquadra é **quem roda o Remotion** (o estúdio/agência), não a cliente. **Decisão 2026-09-18 (usuário)**: uso apenas para estudos, não comercial, ou seja, a cláusula de avaliação. No dia em que um render do Remotion entrar em peça publicada para a cliente, vira uso comercial e o que decide é o quadro de funcionários da agência: até 3, continua grátis; acima, comprar a Company License antes de publicar.
- **Sem rede de fontes no render.** Google Fonts está fora da allowlist; por isso Playfair Display e Montserrat vivem em `assets/fonts/` (TTF variável, OFL) e o `setup.sh` as instala em `~/.fonts` + `fc-cache`. Validado em 2026-09-18: com isso o Remotion e o Chromium resolvem `"Playfair Display"` pelo nome; sem isso caem para a serifa do sistema.
- **A URL do ffmpeg estático quebrou em 18/set/2026 por uma palavra.** `releases/download/latest/…` (tag rolante do BtbN) serve o arquivo; `releases/latest/download/…` devolve 404. O `setup.sh` tenta as duas em ordem e **confere a assinatura XZ (`fd377a585a00`) antes de extrair**. Os 403 do apt são ruído esperado; o que mata é o fallback não entregar o binário.
- **Environment: cada estúdio tem o seu.** Conferido em 2026-09-18 com `get_session` e `list_environments`: esta sessão roda em **`drajulianaromano-conteudo`** (`env_01Xq5rLD9d9CkcogJEj6kbMU`) e **não existe environment "ana-conteudo"** na conta, ao contrário do que este arquivo dizia desde o bootstrap. São sete environments, um por estúdio, mais o Default. Logo, allowlist, variáveis de ambiente e setup script são configurados **repo a repo**; nada que se ajuste aqui vale para os outros estúdios.
- **O campo de setup do environment precisa de caminho absoluto.** O comando de boot roda com o diretório de trabalho no **pai** do repo, então `bash scripts/setup.sh` falha com exit 127 e a sessão nasce sem `/workspace` e sem skills (foi o que aconteceu nesta sessão em 2026-09-18: `/workspace` vazio, setup rodado à mão). Usar a versão que não depende do nome do repo (sobrevive a renomeação; a fixa `bash /home/user/drajulianaromano-conteudo/scripts/setup.sh` também serve, já que o environment é exclusivo deste estúdio):
  ```bash
  for p in ./scripts/setup.sh ./*/scripts/setup.sh; do [ -f "$p" ] && exec bash "$p"; done; p=$(find /home /workspace /repo /app /src -maxdepth 4 -type f -path "*/scripts/setup.sh" 2>/dev/null | head -1); [ -n "$p" ] && exec bash "$p"; echo "setup.sh nao encontrado no repo"; exit 1
  ```
  Linha testada em 2026-09-18 com o diretório de trabalho na raiz do repo, no pai, em `/tmp` e em `/`: roda os seis passos e sai com exit 0 nos quatro. Ver `SETUP.md` para onde colar.
- **Allowlist do environment não cobre subdomínio.** A entrada é literal: `www.googleapis.com` não cobre `generativelanguage.googleapis.com`. Para um site inteiro, `*.dominio.com` junto do apex. Diagnóstico: `curl -sv https://host/ 2>&1 | grep CONNECT`; `HTTP/1.1 403` no CONNECT é allowlist, qualquer outra coisa é chave, quota ou rota. Chromium headless usa o mesmo proxy e não contorna nada.
- **Metricool, rascunho com data vencida não publica e não avisa.** Um post `draft:true` cuja data passa continua aparecendo em `getScheduledPosts` como se estivesse agendado, mas nunca dispara. Quem agenda tira do rascunho na mesma sessão e confirma com `getScheduledPosts`; com a data no passado, precisa de data nova.
- **Chaves de API só por variável de ambiente do environment.** `gera_imagem.py` recusa chave por argumento (vaza em histórico e em `ps`). Os estúdios irmãos registram que variável nova só entra em sessão nova; **aqui não foi assim**: em 2026-09-18 `OPENAI_API_KEY`, `GEMINI_API_KEY` e os três hosts novos apareceram na sessão já aberta, sem reiniciar (`printenv` e CONNECT 200 na mesma sessão que antes via 403). Conferir com `printenv | cut -d= -f1 | grep API_KEY` antes de acusar o script ou de mandar abrir sessão nova. Chave válida não é quota: no Gemini, `429` com `quotaId: ...-FreeTier` significa projeto sem faturamento vinculado; ler o campo `details` do erro. Nesta conta a quota existe: geração real validada em 2026-09-18 nos dois provedores.
- **HyperFrames validado de ponta a ponta em 2026-09-18** (`init`, `add` do registry, `check` verde, `render` MP4 h264 1920x1080 com o bloco visível). Quatro coisas que ninguém adivinha:
  1. **Browser**: `npx hyperframes browser ensure` trava baixando Chrome de host bloqueado. Usar o headless_shell do Playwright por `HYPERFRAMES_BROWSER_PATH=/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell`, que captura por `HeadlessExperimental.beginFrame` (determinístico); o Chromium completo cai para "screenshot mode". O `~/.bashrc` **não basta**, porque tool call roda `bash -c` e não lê o perfil: chamar sempre por **`bash scripts/hyperframes.sh <subcomando>`**.
  2. **O `fetch` nativo do Node ignora `HTTPS_PROXY`.** Com `cdn.jsdelivr.net` já liberado, o compilador ainda levava `HTTP 403` ao baixar o GSAP, porque saía direto no firewall de egresso. A chave é **`NODE_USE_ENV_PROXY=1`** (Node >= 22.21), mais `NODE_EXTRA_CA_CERTS`; o wrapper exporta os dois. Com isso o compilador baixa e **embute** o script no bundle, e o `render` passa mesmo com a tag apontando para o CDN.
  3. **O Chromium não confia na CA do agent proxy.** O banco NSS (`~/.pki/nssdb/cert9.db`) está **vazio** e o `certutil` não pode ser instalado (apt responde 403). Resultado: `check`, `preview` e `validate`, que abrem a página de verdade no browser, falham com `ERR_CERT_AUTHORITY_INVALID` em **qualquer** asset remoto, script ou imagem. Solução, que é a que o próprio HyperFrames recomenda: **asset local**. `bash scripts/vendor_gsap.sh <projeto>` traz o GSAP (do clone do hyperframes ou por `npm pack`) e reaponta todo HTML do projeto, inclusive os blocos em `compositions/`. Nunca desligar verificação de TLS.
  4. **Armadilhas do fluxo**: o scaffold do `init` vem com a animação **comentada**, então o `check` acusa "Timeline did not advance under seek" e o MP4 sai parado (é o HTML, não o browser); e todo host de sub-composição precisa do **`data-composition-id` próprio**, senão o `check` reprova com `host_missing_composition_id`. O `catalog` lista os ~400 blocos (6 itens vêm com manifesto inválido do upstream e são pulados). `doctor` lista whisper-cpp, Kokoro e MusicGen como ausentes: opcionais, a transcrição aqui é da ElevenLabs.
- Testar escopo de chave da ElevenLabs sem gastar crédito: chamar o endpoint com parâmetro inválido. `401 missing_permissions` = escopo ausente; `400`/`404` de validação = escopo presente.

## Grade de publicação de conteúdo curto (set/out 2026)

- **Regra de formato por dia da semana, definida pela chefe da agência (versão final de 2026-09-22)**: segunda **pergunta**, terça **carrossel**, quarta **dica**, quinta **vídeo normal**, sexta **meme**. A primeira leitura ("sexta: carrossel ou meme") foi corrigida: sexta é meme e só meme, carrossel é formato de terça. **Dia sem material fica vazio; nenhum formato substitui outro.** Fim de semana não entra.
- Material do lote `setembro/outubro` (pasta Drive `17Wl5RKCdDBQ77Ck85Zf8MrWcYC_C8urO`): 14 vídeos verticais (6 dicas, 4 perguntas, 3 vídeos normais, 1 já publicado) e 3 carrosséis da pasta `1IkLXfTJm68DMrjRU6nySkJP4k77HAai6`. Contra a semana de 5 slots isso deixa **13 dias vazios até 30/10**, 6 deles de meme (nenhum meme produzido).
- **Legenda nunca copia o texto do vídeo ou do carrossel; ela complementa.** Regra do usuário, dita em maiúsculas em 2026-09-21. Vale para caption de Reel, de carrossel e de meme.
- Carrosséis, em ordem e por data: **C3 "Coisas que eu, como médica, não faria na menopausa"** (8 cards) em terça 22/09; **C2 "Por que tantas mulheres vivem seus anos mais tristes aos 47 e 48"** (7 cards) em terça 29/09; **C1 "Eu acredito que vou viver"** (13 cards) em terça 06/10. Os ids de cada slide no Drive estão catalogados na sessão de 2026-09-21/22 (raspagem do HTML público da subpasta com regex `data-id=` + `data-tooltip=`, que dá nome e id juntos e resolve a ordem dos `_01.._NN`).
- **Carrossel pelo Metricool funciona com Instagram + TikTok no mesmo post.** `media` com a lista ordenada de URLs `drive.usercontent.google.com/download?id=<ID>&export=download&confirm=t`, `instagramData: {type:"POST"}` e `tiktokData: {title, photoCoverIndex:0, autoAddMusic:true}` (TikTok vira post de fotos; `title` é obrigatório). YouTube fica de fora porque não existe Short de imagens. Validado em 2026-09-22 com os 8 slides do C3: os 8 normalizaram de primeira para o CDN do Metricool.
- Agendados/publicados do curto: **379670102** (Reel `L2_05_ROSTO_QUADRADO_REPOSICAO_HORMONAL`, 21/09 18h) **publicado** nos três canais; **380058483** (carrossel C3, 22/09 18h, Instagram + TikTok). Foi para 18h e não para o pico das 10h porque a aprovação chegou às 9h40; na terça o 18h é o segundo melhor horário nas duas redes (Instagram 4788 contra 4942, TikTok 1119 contra 1136), diferença menor que o risco de perder a janela.
- Entregáveis da grade: doc vivo em `https://claude.ai/code/artifact/08d3756f-6569-4d2b-a4f2-8532766fd9fb` mais **PDF e DOCX gerados localmente**, porque o doc aberto no Google Docs perde as imagens (elas são assets `blob/<id>` do artifact, e a rota de abertura leva só o texto). Os geradores ficam no scratchpad: `gera_grade.py` (dados + HTML com imagens em base64), `gera_pdf.py` (Chromium `/opt/pw-browsers/chromium` via playwright, `page.pdf` A4) e `gera_docx.py` (python-docx, imagens embutidas). **Para a chefe abrir no Google Docs, mandar o .docx**, não o link do doc.
- **Divergência de RQE a confirmar com a cliente**: este arquivo registra **RQE 5246** nos IDs da marca, mas as legendas e os posts já publicados usam **RQE 52461**. Um dos dois está errado e é dado de registro profissional. Resolver antes do próximo lote.

## Cinto de ferramentas (portado do `profissioai-conteudo` em 2026-09-18)

Regra do porte: **ferramenta é genérica, marca não é.** Os scripts vieram sem edição (varredura: zero referências à marca de origem); o que carrega marca ficou em 3 lugares, todos desta marca agora: `remotion/src/marca.ts` (paleta e fonte), `remotion/src/Root.tsx` (props padrão e rodapé `@drajulianaromano`) e o bloco 7 do `validate.sh` (blog_id 6741531). `setup.sh` já era agnóstico (symlink pelo nome da pasta). Guia de origem: `PORTAR.md` no repo `suportezv/profissioai-conteudo` (branch `claude/reels-automaticos-profissio-d0qk2d`).

| Ferramenta | Uso | Depende de |
|---|---|---|
| `scripts/decupar.py edl.json --brutos DIR --stt DIR --lut X.cube --saida DIR` | decupagem por âncoras de texto, junta trechos, gira, LUT, normaliza áudio | ffmpeg, transcrição Scribe com timestamps |
| `scripts/relatorio_decupagem.py` | relatório do que ficou e caiu, retranscrevendo as peças finais | `ELEVENLABS_API_KEY` |
| `scripts/gera_lut_slog2.py saida.cube` | LUT 3D S-Log2/S-Gamut para Rec.709 | `colour-science` (o passo 5 do `setup.sh` instala; validado: LUT 17^3 gerada e aplicada) |
| `scripts/zip_index_remoto.py list\|get <file_id>` | lista/extrai de ZIP gigante no Drive por Range, sem baixar o ZIP | arquivo público no Drive |
| `scripts/gera_imagem.py openai\|gemini "prompt" saida.png` | imagem por GPT ou Gemini, mesma interface | `OPENAI_API_KEY` / `GEMINI_API_KEY` + hosts liberados (tudo OK desde 2026-09-18; geração real validada com `gpt-image-1-mini` e `gemini-2.5-flash-image`; padrões do script são `gpt-image-2` e `gemini-3-pro-image`, mais caros) |
| `scripts/sobe_para_drive.py --pasta <id> arquivos` | upload para pasta do Drive com token pronto | `www.googleapis.com` (liberado em 2026-09-18) + `GOOGLE_OAUTH_TOKEN` sob demanda; upload real ainda não testado |
| `remotion/` (`npx remotion still\|render src/index.ts CartaoTituloVertical ...`) | cartões de título 1080x1920 e 1080x1080 com fundo aurora na paleta da marca | `npm install` no setup, headless_shell. Validado: still e MP4 completo (150 frames h264, 47 s) em 2026-09-18 |
| HyperFrames, sempre por `bash scripts/hyperframes.sh <init\|add\|check\|render>` | fluxo com as 20 skills (brief, storyboard, registry de ~400 blocos, legendas, render) | wrapper (browser + proxy do Node) e `bash scripts/vendor_gsap.sh <projeto>` antes do `check` |
| `scripts/vendor_gsap.sh <projeto>` | traz o GSAP para dentro do projeto e reaponta todo HTML | clone do hyperframes ou `npm pack` |

Estado do environment em 2026-09-18 (validado nesta sessão, quatro rodadas): `ELEVENLABS_API_KEY`, `OPENAI_API_KEY` e `GEMINI_API_KEY` presentes; allowlist cobre `api.openai.com`, `generativelanguage.googleapis.com`, `www.googleapis.com`, `cdn.jsdelivr.net` e `raw.githubusercontent.com` (todos liberados no meio da sessão e já respondendo), além de `drive.google.com`, `drive.usercontent.google.com`, `api.elevenlabs.io`, `github.com`, `api.github.com`, `pypi.org` e `registry.npmjs.org`. **Nenhum host pendente.** O que ainda limita é a CA do proxy no Chromium, não a rede. Kairogen segue FREE com 0 créditos (`get_me_context`). Metricool (`getBrandSettings`, duas chamadas no mesmo dia): a marca `drajulianaromano` (6741531) está certa, com Instagram, TikTok e YouTube; o que o JSON mostra de errado é **na marca `drjulianofratezi` (6741542)**, cujo `networksData` traz `instagramData: "drajulianaromano"`. O usuário conferiu o perfil da drajulianaromano e está correto; o perfil a abrir no painel é o da drjulianofratezi. Se lá aparecer o Instagram certo, o campo da API está desatualizado e a nota pode ser apagada.

Prova por ferramenta (2026-09-18): **Remotion** still e MP4 completo nos dois formatos (1080x1920 e 1080x1080) com Playfair; **OpenAI** imagem real com `gpt-image-1-mini` e com o padrão `gpt-image-2`; **Gemini** imagem real com `gemini-2.5-flash-image` e com o padrão `gemini-3-pro-image` (sai 1408x768); **HyperFrames** projeto novo, bloco `camcorder-hud` instalado do registry, `check` verde e MP4 com o bloco visível no frame. Não testado: upload real com `sobe_para_drive.py` (precisa de token OAuth gerado na hora) e `decupar.py` com bruto real (precisa de transcrição Scribe de um vídeo).
